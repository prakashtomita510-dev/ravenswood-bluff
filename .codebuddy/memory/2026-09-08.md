# 2026-09-08

## 任务：项目全景梳理 + 长期路线图与执行手册制定

**前置核实**：PLN-041/042/043 主体代码已全部提交（`1543cea`~`42714c1` 区间），PROGRESS 任务 24/25/26「待用户确认后 commit」实为已提交；未提交的是上次会话 RPT-020 收尾产物（RPT-020 报告 / REV-011 提示词 / `data/blind_ready3/`）。

**产出 1 — `docs/plans/pln044-long-term-roadmap.md`（PLN-044，路线图）**
- §1 现状全景：质量基线（快速 710 / 全量 676 / 10 gate / live fallback 2.5% / per-action 1660.9）+ 7 类能力资产 + 12 项遗留问题（RPT-020 §8 为主）+ 短板与机会。
- §2 愿景 + 6 项北极星指标（拟人度/对局质量/进化有效/内容/规模/成本，含当前值）。
- §3 三主线方向决策：**D 工程质量 → A 认知纵深 → B 内容广度 → C 体验规模**（先淬火再认知，认知是差异化核心且能力链刚建成；内容依赖认知架构稳定；规模在内容足够后放大）。
- §4-§9 五里程碑：Alpha 1.2.x 淬火（T1-T9 可直接执行）/ 1.3 深潜（T1-T8 认知统一+双向观点+动态 RAG+LLM 蒸馏）/ 1.4 新月（T1-T7 BMR 第二剧本）/ 1.5 广场（T1-T5 复盘 UI+12 人局+并发）/ Beta 2.0 开门（T1-T5 公测）。
- §10 九条跨阶段红线；§11 滚动规划机制（里程碑启动细化 / 收尾回顾 / 触发式回顾）。

**产出 2 — `docs/plans/pln045-execution-handbook.md`（PLN-045，执行手册）**
- 启动链（增补读路线图定里程碑）、9 条红线、生命周期总览。
- SOP-1 里程碑启动 / SOP-2 任务循环（TDD + 编码规范 + 提交粒度）/ SOP-3 四层门禁 L1-L4（含 PowerShell 命令速查）/ SOP-4 收尾与版本发布 / SOP-5 路线图回顾。
- 文档登记规范表（PLN/DECISIONS/RPT/REV/REL/PROGRESS/MEMORY 各自时机）、陷阱速查 11 条、决策升级规则 7 条、附录（PLN 骨架模板 + 基线数字）。

**同步改动**：`docs/README.md`（索引登记 PLN-044/045 + 场景查找表加行）；`PROGRESS.md`（头部日期、任务 24/25/26 状态修正、新增任务 27、未提交清单 2026-09-08 登记、会话记录行）；`AGENTS.md`（上班清单第 5 步读路线图 / 第 6 步按手册执行 + 专题索引加两行）。

**验证**：`scripts/check_doc_health.py` PASSED（95 md，仅 1 个历史非致命绝对链接 warning）。纯文档改动，未跑 pytest/ruff（无代码变更）。

**待办**：提交须用户确认（铁律）。下一步按 PLN-044 §5 启动 Alpha 1.2.x「淬火」——T1 文档清账（提交上述遗留）→ T2 decide 节点超时对齐（RPT-020 P1，live 5% 工作流失败根因）。

## 追加：doc-governance skill 文档治理审计（REV-014）

用户加载 doc-governance skill，按其五维工作流执行「诊断 + 审计 + 腐败检测」，产出 `docs/reviews/doc-governance-audit-2026-09-08.md`（REV-014）。

**发现与修复**：
- **幽灵文档 4 篇**（未在 docs/README.md 注册）：`reviews/cr-review-pln041-042-2026-08-13.md`（改 doc_id REV-012 + role [State]→[Delta] 修正）、`reviews/cr-review-pln043-2026-08-14.md`（改 REV-013）、`plans/token-budget-optimization-plan.md`、**doc_id 冲突**（原 PLN-037 与 agent-native-redesign-plan.md 双占 → 新分配 PLN-046）、`releases/v0.8/AGENTS_refactor.md`（补 REL-008，补全编号序列）。
- **巨型单文件 3 篇**（>500 行）：harness-engineering-analysis 979 / alpha-1.1-plan 622 / task_m5_ai_speed_flow 524 → 登记豁免（历史 Delta/Cold 文档，拆分 churn > 收益）。
- **role 偏差 4 处**：2 处修正（cr-review-pln041-042）、2 处已知豁免（guides 下 2 份 RPT 文档 + AGENTS_refactor 设计说明）。
- **PLN-044/045 合规修复**：补 `tags`+`related`、加 TOC、代码块补语言标注（text/markdown/powershell）、交叉引用改同目录相对路径。评分：Agent 友好度 100/100、人类可读性 92/100、体系综合 89/100。

**代码一致性抽查**：`ToolCallNode.timeout_seconds=10.0`（`workflow.py:50`）且 action_workflows 未覆盖 → PLN-044 T2 前提成立 ✓。

**清洁状态**：一次性扫描脚本与输出已删除；doc health PASSED（96 文件，仅 1 个历史绝对链接 warning）。

**经验**：① Windows 跑含 emoji 的 python 脚本输出会 GBK 报错 → `sys.stdout.reconfigure(encoding="utf-8")` + 用 ASCII 标记；PowerShell 管道仍可能乱码，输出重定向到文件再 read_file。② 项目 doc_id 前缀制（PLN/RPT/REV/REL/REF）与 skill 默认 `{category}-{NNN}` 属同一模型两种实例，审计以项目约定为准，只要求编号唯一 + 前缀对应 category + 必注册索引。③ 建议后续把「幽灵文档 + 巨型单文件」检测并入 `scripts/check_doc_health.py` 常态化。（当日已落地，见下）

## 追加：文档治理检查常驻 CI + 提交推送（用户授权）

**实现**（`scripts/check_doc_health.py`）：新增 `MAX_DOC_LINES=500` / `INDEX_NAME="README.md"` 常量 + `check_index_registration()`（幽灵文档，**HARD failure**，RC=1 阻断 CI）+ `check_doc_size()`（>500 行 → warning，`--strict` 升级为 failure）；main 中预读索引文本（缺失则 skip 并 stderr 告警），warning 汇总文案改为「non-portable links / oversized documents」，PASSED 文案加 `index registered`。

**验证**：默认 PASSED/RC=0（4 warnings = 3 oversized + 1 历史绝对链接）→ 植入幽灵探针 `docs/plans/zz-ghost-probe.md` → FAILED/RC=1（命中 ghost document）→ `--strict` RC=1 → 删除探针后恢复 RC=0。`ruff check scripts` 0 告警。探针与临时输出已清理。

**踩坑**：本机 `ruff` 不在 PATH → 必须用 `.\.venv\Scripts\python.exe -m ruff check ...`；python 脚本输出经 PowerShell 管道仍可能乱码，统一 `| Out-File -Encoding utf8` 后 read_file。

**提交**（用户明确授权 push）：按主题 5 个原子 commit —— ① `feat(scripts)` 门禁增强；② `docs(pln044-045)` 路线图+手册+AGENTS 启动链；③ `docs(rev014)` 治理审计+幽灵文档/doc_id 冲突治理；④ `docs(rpt020)` live 效果分析+盲测样本+索引/进度同步；⑤ `chore(memory)` 工作记忆。随后 push origin/main。
